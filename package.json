{
  "name": "storkSQL",
  "version": "1.0.2",
  "description": "A SQL ORM with Mongoose-like syntax. Built on top of pgp-promise.",
  "main": "./dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel ./src -d ./ --presets es2015,stage-2 --copy-files",
    "pp": "npm run build; npm version patch; npm publish"
  },
  "author": "Alex Stark",
  "license": "ISC",
  "dependencies": {
    "bluebird": "^3.4.1",
    "lodash": "^4.14.0"
  }
}
